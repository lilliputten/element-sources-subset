block('popup_dialog')( elem('title')(

    /*

    def()(function(){
        var ctx = this.ctx;
        return applyNext();
    }),

    js()(function(){
        var ctx = this.ctx;
        var id = ctx.id; // if defined?
        return { id : id };
    }),

    */

    content()(function(){
        var ctx = this.ctx;
        var options = ctx.options;

        var content = options.title;

        return content;
    }),

    def()()

));

